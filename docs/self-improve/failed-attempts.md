# Failed attempts

When a cycle abandons a change because it broke something or the panel talked it
out of the approach, it records the attempt here so a later cycle does not walk
into the same wall. One entry per dead end: what was tried, why it failed, and what
would need to change for it to be worth revisiting.

(Nothing yet.)
